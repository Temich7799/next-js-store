import * as React from "react"
import SocialsList from "../SocialsList/SocialsList"

const Header = () => {
    return (
        <header>
            <SocialsList />
        </header>
    )
}

export default Header
